# Example environment variables for mem0 Supabase memory test
# Copy this file to `.env` and fill in real values, or set these variables in your shell.

# Supabase Postgres connection string (must start with postgresql://)
# You can copy this from Supabase: Project Settings -> Database -> Connection string
SUPABASE_DB_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME

# OpenAI API key used by mem0 for LLM (fact extraction) and embeddings
OPENAI_API_KEY=sk-your-openai-api-key

# Optional: override default models used by mem0 for testing
# Small, cheap LLM for memory extraction
MEM0_LLM_MODEL=gpt-4.1-nano-2025-04-14
# Embedding model and its dimensions (must match)
MEM0_EMBEDDING_MODEL=text-embedding-3-small
MEM0_EMBEDDING_DIMS=1536

# Optional: collection name in Supabase (table/index namespace)
MEM0_COLLECTION_NAME=mem0_memories

# Optional: test user id used in the example script
MEM0_TEST_USER_ID=test-user
